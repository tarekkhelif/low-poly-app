/* eslint-disable react/no-multi-comp,
    react/jsx-indent,
    react/jsx-indent-props,
    react/prop-types */

import React from "react";
import { connect } from "react-redux";

// const mapStateToProps = ({ patches }, { id }) => ({

// })
// export const Patch = connect(mapStateToProps)(({ polygons }) => {
//     polygons.forEach(({ id, nodes, color }) => (
//         <Polygon key={id} id={id} points={} color={color} />
//     ))
// })
